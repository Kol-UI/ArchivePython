#class foxAndHounds

from gameBoard.py import *
from Hounds.py import *
from Fox.py import *

class fowAndHounds(self, plateauJeu, canMove, deplacementHounds, deplacementFox):
    def __init__(self, nbLigne):
        self.__nbLigne = 8

    def play():
        pionSelectionne = int(input(print('Selectionnez un de vos pions: ')))
        return Hounds.coordonéesCase
        while win = False:
            Hounds.move()
            Fox.move()
            if win = True:
                victory()
        break
                
    def victory():        
        if canMove && self._deplacementFox == True
            if win == True
                print('Le Fox a gagné !')
        else;
            print('Les Hounds ont gagné !')

play()
